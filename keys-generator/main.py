"""
Generate private and public key with works in Azure APIM with validate-jwt
"""
import logging


def main():
    logging.basicConfig(level=logging.DEBUG)
    logging.info("Generate keys")

if __name__ == "__main__":
    # execute only if run as script
    main()
